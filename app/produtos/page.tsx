import { ProductCard } from "../components/product-card";
import { StoreHeader } from "../components/store-header";
import { getCatalog } from "../../lib/catalog/repository";
export default async function ProductsPage(){const {products,error}=await getCatalog();return <><StoreHeader/><main className="catalog-page"><div className="catalog-heading"><p>Coleção atual</p><h1>Novidades</h1><span>Peças selecionadas para renovar seu guarda-roupa.</span></div><div className="catalog-toolbar"><strong>{products.length} produtos</strong><span>Mais recentes</span></div>{error?<p className="catalog-error">{error}</p>:products.length?<div className="product-grid">{products.map(product=><ProductCard key={product.slug} product={product}/>)}</div>:<p className="catalog-error">Nenhum produto publicado.</p>}</main></>}
